# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Loom + configuration cache still do not get along perfectly.
org.gradle.configuration-cache=false

# Fabric properties (verified working combination for Minecraft 1.21.1)
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.16.10
loom_version=1.7-SNAPSHOT

# Mod properties
mod_version=1.0.0
maven_group=com.spiderman
archives_base_name=spiderman

# Dependencies
fabric_api_version=0.105.0+1.21.1
